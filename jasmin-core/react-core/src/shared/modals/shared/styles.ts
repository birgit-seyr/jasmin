import type { CSSProperties } from "react";

/**
 * Shared visual style constants used by modal forms. Keeping them in one
 * place avoids drift between the various edit modals.
 */
export const greenInputStyle: CSSProperties = {
  backgroundColor: "#f0fdf4",
};
