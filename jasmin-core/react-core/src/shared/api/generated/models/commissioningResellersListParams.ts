/**
 * Generated by orval v7.21.0 🍺
 * Do not edit manually.
 * Jasmin Platform API
 * CSA Management Platform API
 * OpenAPI spec version: 1.0.0
 */

export type CommissioningResellersListParams = {
/**
 * Day of the week (0=Monday, 6=Sunday) the `has_orders` annotation is scoped to. `has_orders` is annotated whenever at least one of `year`, `delivery_week` and `day_number` is sent, scoped to those that are.
 * @minimum 0
 * @maximum 6
 */
day_number?: number;
/**
 * Deprecated alias of `day_number` for this endpoint: the value is matched against the order's day index (0-6), never against a SharesDeliveryDay id. Send `day_number` instead; when both are present `day_number` wins.
 * @minimum 0
 * @maximum 6
 */
delivery_day?: number;
/**
 * ISO week number (1-53). Week 53 is accepted only for a year that has one; sent with a 52-week year it is refused.
 * @minimum 1
 * @maximum 53
 */
delivery_week?: number;
/**
 * Keep only resellers that still have at least one order no invoice covers (false keeps only those with none). Scoped to the order year when `year` is also sent
 */
has_orders_without_invoice?: boolean;
/**
 * Filter by active donation recipient status
 */
is_active_donation_recipient?: boolean;
/**
 * Filter by active reseller status
 */
is_active_reseller?: boolean;
/**
 * Filter by active seller status
 */
is_active_seller?: boolean;
/**
 * Filter by active supplier status
 */
is_active_supplier?: boolean;
/**
 * Filter by donation recipient flag
 */
is_donation_recipient?: boolean;
/**
 * Filter by reseller flag
 */
is_reseller?: boolean;
/**
 * Filter by seller flag
 */
is_seller?: boolean;
/**
 * Filter by supplier flag
 */
is_supplier?: boolean;
/**
 * Page size. Pass it to opt into pagination: the response is then `{count, next, previous, results}` instead of a plain array. A value above 1000 is served at 1000 rather than refused.
 * @minimum 1
 */
limit?: number;
/**
 * Index of the first row to return. Only meaningful together with `limit`.
 * @minimum 0
 */
offset?: number;
/**
 * Year (YYYY format)
 * @minimum 1900
 * @maximum 2100
 */
year?: number;
};
