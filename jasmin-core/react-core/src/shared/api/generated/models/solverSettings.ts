/**
 * Generated by orval v7.21.0 🍺
 * Do not edit manually.
 * Jasmin Platform API
 * CSA Management Platform API
 * OpenAPI spec version: 1.0.0
 */

export interface SolverSettings {
  readonly id?: string;
  readonly is_active?: boolean;
  /**
   * Wall-clock budget for ONE solve. A hard plan legitimately needs 10+ minutes, so this goes up to 2 hours; the default stays low for quick feedback. A run computes several plans, so the total is this times the number of solutions.
   * @minimum 1
   * @maximum 7200
   */
  solver_max_time_seconds?: number;
  /**
   * CP-SAT search worker threads (num_search_workers).
   * @minimum 1
   * @maximum 32
   */
  solver_workers?: number;
  /**
   * How many distinct candidate plans one run produces.
   * @minimum 1
   * @maximum 20
   */
  default_num_solutions?: number;
  /** Within one bed, crops growing at the same time must share a planting line (hard constraint). */
  enable_planting_line_homogeneity?: boolean;
  /** Cover fleece-needing crops with 4-bed-wide fleece units (hard) and minimise fleece-weeks (soft). Expensive: adds per-(plot, week, bed) variables. */
  enable_fleece?: boolean;
  /** Keep beds sharing a planting line near each other (soft, aesthetic). The priciest soft term. */
  enable_line_dispersion?: boolean;
  /**
   * Consolidate the plan onto as few plots as possible.
   * @minimum 0
   * @maximum 1000
   */
  weight_plots_used?: number;
  /**
   * Fewer distinct beds across the season (rewards succession).
   * @minimum 0
   * @maximum 1000
   */
  weight_beds_used?: number;
  /**
   * Keep each batch bed-aligned (spanning fewest beds). Defaults to 0: a crop may run from one bed into the next, and forcing bed-aligned starts strands the leftover cells of every bed, which wastes land.
   * @minimum 0
   * @maximum 1000
   */
  weight_beds_per_batch?: number;
  /**
   * No gaps between the first and last used bed of a plot.
   * @minimum 0
   * @maximum 1000
   */
  weight_compact_span?: number;
  /**
   * Group beds that share a planting line.
   * @minimum 0
   * @maximum 1000
   */
  weight_line_dispersion?: number;
  /**
   * Minimise how many fleece-weeks the plan needs.
   * @minimum 0
   * @maximum 1000
   */
  weight_fleece_count?: number;
  readonly updated_at?: string;
}
