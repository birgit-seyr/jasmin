Viewset Tests Overview
======================
145 tests | 36 test classes | 8 files
Run: poetry run pytest apps/commissioning/tests/tests_viewsets/ --no-header --tb=short -q


test_badge_viewsets.py (9 tests)
────────────────────────────────
  TestUnConfirmedSubscriptionsViewSet     3 tests   unconfirmed_subscriptions badge count
  TestUnConfirmedTrialSubscriptionsViewSet 3 tests  unconfirmed_trial_subscriptions badge count
  TestUnConfirmedMembersViewSet           3 tests   unconfirmed_members badge count

test_basics_viewsets.py (36 tests)
──────────────────────────────────
  TestSeasonViewSet                       3 tests   list, create (with valid_from/valid_until)
  TestStorageViewSet                      5 tests   list, isPast active/inactive filter, ordering
  TestShareArticleViewSet                 3 tests   list, isPast active filter
  TestDefaultShareArticleInShareViewSet  19 tests   list/filter (article/variation/type), CRUD,
                                                    bulk_upsert (create/update/delete on null|0,
                                                    fallback unit, explicit unit, scope filter,
                                                    404 on missing share_article, 400 on bad payload),
                                                    permissions (member/anon)
  TestShareArticleNetPriceDestroyGuard    3 tests   DELETE enforces can_be_deleted: active price of
                                                    in-use article → 409 + row kept; unused → 204;
                                                    future price of in-use article → 204

test_choices_viewsets.py (8 tests)
──────────────────────────────────
  TestSharesDeliveryDayViewSet            3 tests   list, retrieve
  TestOrdersDeliveryDayViewSet            2 tests   list
  TestPaymentCycleViewSet                 3 tests   list, is_active filter

test_delivery_viewsets.py (21 tests)
────────────────────────────────────
  TestDeliveryStationViewSet              7 tests   list, filter is_active, retrieve, filter by member (confirmed only), member-role read
  TestDeliveryStationDayViewSet          11 tests   list, filter by station/day, capacity by week
  TestDeliveryToursViewSet                3 tests   requires delivery_day param, list with day, update_tours targets open row on versioned station-day

test_documentation_viewsets.py (19 tests)
─────────────────────────────────────────
  TestPlotViewSet                         6 tests   list, filter is_active, create, retrieve, delete
  TestWasteViewSet                        2 tests   list
  TestHarvestViewSet                      2 tests   list
  TestPurchaseViewSet                     2 tests   list
  TestForecastViewSet                     3 tests   list, filter by year/week
  TestBulkSetAsExpected                   4 tests   harvest/purchase set-as-expected valid (204);
                                                    malformed item 400 (serializer.is_valid wiring)

test_members_viewsets.py (15 tests)
───────────────────────────────────
  TestMemberViewSet                       8 tests   list, filter active/trial, confirm, reject, retrieve
  TestSubscriptionViewSet                 4 tests   list, filter is_trial, filter by member
  TestCoopShareViewSet                    3 tests   list, filter by member

test_shares_viewsets.py (32 tests)
──────────────────────────────────
  TestShareTypeViewSet                    7 tests   list, filter share_option, create/update uppercase, invalid 400
  TestShareTypeVariationViewSet           5 tests   list, filter by share_type/physical/internal_name
  TestShareTypeVariationGrossPriceViewSet 3 tests   list, filter by variation
  TestShareTypeVariationGrossPriceDestroyGuard 2 tests  DELETE enforces can_be_deleted: price of a
                                                    subscribed variation → 409 + row kept; unsubscribed → 204
  TestShareViewSet                        4 tests   list, filter year/week, get_days action
  TestDefaultShareContentBulkCreateValidation 3 tests  valid amount_<var> cell persists (200); missing field 400; NaN/non-finite 400
  TestDefaultShareContentBulkUpdateValidation 2 tests  composite-id PUT replaces slot amount; bad-typed field 400
  TestHarvestSharePlanningViewSet         4 tests   create persists ShareContent + station; null note ok;
                                                    missing field 400; no-amounts 400
  TestVirtualComponentsViewSet            3 tests   create 201 + persisted component; missing virtual_variation 400;
                                                    non-physical component 400

test_resellers_viewsets.py (20 tests)
─────────────────────────────────────
  TestResellerViewSet                     6 tests   list, filter is_reseller/is_active_reseller, retrieve, delete
  TestOfferGroupViewSet                   2 tests   list
  TestOfferViewSet                        3 tests   list, filter by year/week
  TestOrderContentViewSet                 6 tests   list, create derives tax_rate, Orders page offer/article row
                                                    create+PATCH payloads, customer page payloads via staff route,
                                                    PATCH without amount keeps amount/stock/crate row
  TestInvoiceResellerViewSet              6 tests   list, upload_pdf requires finalized, create_storno guards, protected fields read-only on update
  TestDeliveryNoteResellerViewSet         3 tests   list, upload_pdf requires finalized
  TestCommissioningListResellersViewSet   2 tests   requires params, returns for valid params

test_consents_viewsets.py (32 tests) — DSGVO-critical access matrix
───────────────────────────────────────────────────────────────────
  TestConsentDocumentReadAccess           6 tests   anon + member + office all 200; current/ 200 + clean 404
  TestConsentDocumentWriteAccess          6 tests   anon 401/403, member 403; office 201; PROTECTed delete → 409
  TestConsentDocumentImmutableOnceConsented 10 tests  PATCH/PUT changing body/title/kind/locale/version/valid_from
                                                    of a consented doc → 409 consent.document_immutable, row +
                                                    sha unchanged; unchanged values → 200; draft edit → 200 +
                                                    stored PDF re-rendered
  TestConsentRecordCreate                 7 tests   member pinned to self; spoof attempt → 403; office can act on behalf; IP from X-Forwarded-For; UA captured; unknown document → 404
  TestConsentRecordListScoping            4 tests   anon blocked; member sees only own; office sees all; ?member= filter
  TestConsentRecordRevoke                 5 tests   anon blocked; owner revokes own; non-owning member → 404 (not leaked); office revokes any; double-revoke → 409

test_server_owned_field_locks.py (18 tests) — generic writes cannot set server-owned columns
────────────────────────────────────────────────────────────────────────────────────────────
  TestInvoiceLineLocks                    3 tests   PATCH cannot re-point a draft line onto a finalized invoice; finalization / source_* / order_content / delivery_note_contents dropped; create still takes invoice
  TestDeliveryNoteLineLocks               2 tests   PATCH cannot re-point or stamp a line; create still takes delivery_note
  TestOfferLocks                          1 test    PATCH cannot finalize an offer
  TestInvoiceRecipientSnapshotLock        1 test    PATCH cannot plant recipient_snapshot / document_hash_version on a draft
  TestShareContentLocks                   2 tests   PATCH cannot unfinalize or rewrite authorship; create stamps request user
  TestDocumentationLocks                  3 tests   harvest PATCH locks finalization + authorship; harvest create stamps request user; theoretical harvest authorship
  TestSubscriptionLocks                   1 test    PATCH cannot rewrite renewal chain / waiting-list status / authorship (frozen clock)
  TestMemberLocks                         2 tests   PATCH cannot set consent / cancellation / waiting-list / audit stamps; create stamps request user
  TestShareDeliveryLocks                  3 tests   overview create ignores display annotations; PATCH cannot re-point subscription (both viewsets)

test_coop_share_amount_and_confirmed_lock.py (16 tests) — coop-share amount rule + confirmed-share edit lock
─────────────────────────────────────────────────────────────────────────────────────────────────────────
  TestOfficeCoopShareAmount               6 tests   office create -2 / 0 / 2.5 → 400 coop_share.invalid_amount; whole amount 201; PATCH to a fractional amount → 400; unchanged legacy fractional amount stays editable
  TestConfirmedCoopShareEditLock          8 tests   PATCH amount / is_increase / due_date / member on a confirmed share → 409 coop_share.confirmed_fields_locked; PUT rewriting amount → 409; office-grid bookkeeping save (paid_back_date + note, re-sent tenant value) → 200 keeping the value snapshot; unconfirmed terms editable; confirm committed after load still locks (row re-read under lock)
  TestCoopShareUpdateBoundsLock           2 tests   update takes the per-member coop_share_bounds advisory lock; reassignment locks both members in sorted order

test_coop_share_transfer.py (30 tests) — office coop share transfer between members as ledger rows (frozen clock)
─────────────────────────────────────────────────────────────────────────────────────────────────────────────────
  TestCoopShareTransfer                  15 tests   negative row for the giver + positive row for the receiver (confirmed, paid on the transfer date, notes, existing rows untouched); share value with the most recent payment first; a later transfer only gives what is left; a backdated transfer is capped per share value by today's holding; giving every share needs confirm_member_cancellation, then settles the giver's rows (no payback date, settled_by_transfer) and cancels the giver with a no-settlement email; pending shares don't keep such a giver; no GDPR retention block after a full transfer; a partial giver who leaves later owes only the rest (retention + payback statistics); member register counts transfer rows from the transfer date; giver left between 0 and the minimum → 400 member.coop_shares_out_of_range, nothing written; receiver above the maximum → 400; active subscriptions keep everything; a backdated transfer to a trial member uses the transfer date as entry date
  TestCoopShareTransferValidation        15 tests   more than the confirmed, paid shares → 400 exceeds_held; shares paid after the transfer date don't count; same member; giver not admitted; receiver pending / rejected; cancelled receiver → 400; cancelled giver → 409; future date; date before the giver's or the receiver's entry date (details.context names the side); amount 0 / -1 / 2.5 → 400; anonymous refused

test_subscription_price_bounds.py (12 tests) — office subscription quantity/price bounds + solidarity floor on PATCH (frozen clock)
────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
  TestOfficeSubscriptionBounds            6 tests   create quantity 0 / -1 → 400; price -1 → 400; PATCH quantity 0 → 400; price 0 and a free trial still 201
  TestSolidarityFloorOnPatch              6 tests   PATCH variation / valid_from / is_trial re-checks the STORED price against the new floor → 400; variation + price at the floor → 200; unrelated edit of a below-floor draft → 200; solidarity off keeps price discretion

test_picture_upload_validation.py (37 tests) — picture uploads must be raster images
────────────────────────────────────────────────────────────────────────────────────
  TestPictureUploadViaUpdate             24 tests   ShareTypeVariation + DeliveryStation PATCH: html / svg / markup-named-.png → 400 picture_invalid; PNG/JPEG/WEBP/GIF stored re-encoded with matching extension; multi-picture (MPO) JPEG stored as .jpg; PNG named x.html stored as .png; byte cap + pixel cap → 400; null PATCH still clears
  TestPictureUploadViaCreate              3 tests   DeliveryStation POST rejects html / stores PNG under .png; ShareTypeVariation POST rejects svg
  TestNormalizeUploadedPicture           10 tests   shared helper: None/"" pass through; truncated image rejected; generated name ignores client name; CMYK→RGB drops ICC profile, unconverted keeps it; EXIF orientation applied + EXIF dropped; P-mode PNG/GIF transparency kept; 16-bit PNG kept
